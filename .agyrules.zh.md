# AI-EOS Antigravity 全局规则

你正在 AI 工程平台 (AI-EOS) 中运行。你的主要角色跨越 Phase 1 (知识) 到 Phase 4 (规范) 以及 Phase 7 (知识捕获)。

## 1. 上下文感知
在提出架构建议或生成代码之前，你必须始终检查现有的设计文档是否涵盖了该主题：
- `07-Architecture/` 用于高级循环和交互设计。
- `08-Specification-Driven-Development/` 用于严格的 BDD 行为。
- `06-Standards/` 用于特定语言的规则。

## 2. 没有规范就不写代码
如果用户要求你用 C# 或 F# 实现新功能，你应该首先在 `08-Specification-Driven-Development/` 中输出规范 (BDD)，并让用户同意。只有在规范通过后，才能在 `implementations/` 中编写代码。

## 3. 语言分离
保持架构与实现完全分离：
- 如果被问及架构问题，请更新 `07-Architecture/` 中的 Markdown 文件。
- 如果做出了决定，请使用 ADR 模板在 `07-Architecture/ADR-XXX.md` 中创建 ADR。
- F# 和 C# 是同一架构的双重实现。确保两者保持对等。

## 4. 文档优先
如果你解决了一个复杂的 Bug 或找出了一套新工作流，你必须在 Phase 7 中捕获该知识，更新 `03-Knowledge-System/` 中相关的文档，或在 `05-Prompt-Library/` 中添加一个可重用的提示词。

## 5. 双语文档
在生成或更新文档时，你必须始终生成两个版本：
- 英文版本（使用标准的 `.md` 命名）。
- 中文版本（使用 `.zh.md` 后缀）。
