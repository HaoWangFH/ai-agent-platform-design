# AI-EOS Antigravity 全局规则指南

你正在 AI Agent 平台 (`Skight.AgentPlatform`) 框架下运行。你的核心职责是跨越 SDLC 全生命周期的 7 个阶段（阶段 1 分析至阶段 7 知识沉淀）。

## 1. 上下文感知与阶段导航 (Context Awareness & Phase Navigation)
在提供架构建议或生成代码前，你必须首先检查各 SDLC 阶段现有文档：
- **阶段 1 (分析)**：`03-Analysis/` 用于需求分析、差距分析与标杆竞品研究。
- **阶段 2 (设计)**：`04-Architecture/` 用于高级循环架构与交互设计。
- **阶段 3 (规范)**：`05-Specification-Driven-Development/` 用于严格的 BDD 规范行为与验收标准。
- **阶段 4 (规划)**：`06-Tasks/` 用于 Master 任务 Backlog、Sprint 迭代项与进度追踪。
- **阶段 5 (实施)**：`implementations/` 用于 C# (`implementations/csharp/`) 与 F# (`implementations/fsharp/`) 代码构建。
- **阶段 6 (验证)**：`07-Testing/` 用于自动化测试套件 (Expecto, xUnit) 与代码质量止步门禁 (`pre_verify`)。
- **阶段 7 (知识沉淀)**：`08-Knowledge-System/` 用于实施后业务知识与术语字典沉淀。
- **标准与基础设施**：`09-Standards/` 编码规范、`10-Skill-Library/` 技能库、`11-Prompt-Library/` 提示词库、`15-Cloud-Infrastructure/` 云部署配置。

## 2. 无规范不代码 (No Code Without Specification)
如果用户要求你在 C# 或 F# 中实现新特性，你必须首先在 `05-Specification-Driven-Development/` 中输出 BDD 测试规范，并在获得用户确认后，方可在 `implementations/` 下编写代码。

## 3. 架构与实现解耦 (Dual-Language Architecture & Parity)
保持架构设计与具体的代码实现完全分离：
- 若解答架构问题，请更新 `04-Architecture/` 下的 Markdown 文档。
- 若作出架构决策，请使用 ADR 模板在 `04-Architecture/ADR-XXX.md` 中创建记录。
- F# 与 C# 是同一套架构的双语言实现，确保两者具备对等的特性支持。

## 4. 文档优先与知识沉淀 (Documentation First & Knowledge Capture)
如果你解决了一个复杂的 Bug 或探索出了一套新工作流，你必须在阶段 7 中将该知识沉淀到 `08-Knowledge-System/`，或在 `11-Prompt-Library/` 中添加可重用的 Prompt。

## 5. 双语文档规范 (Bilingual Documentation Rule)
生成或更新文档时，你必须始终同时提供两个版本：
- 英文版本（使用标准的 `.md` 命名）。
- 中文版本（使用 `.zh.md` 后缀）。
